{
  "name": "kaboom-parcel-setup",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist/ && rm -rf .cache/ && parcel src/index.html -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brian Nunez",
  "license": "ISC",
  "dependencies": {
    "kaboom": "^0.5.1"
  },
  "alias": {
    "kaboom": "./node_modules/kaboom/dist/kaboom.mjs"
  },
  "staticFiles": {
    "staticPath": "src/assets",
    "outDirPattern": "sprites",
    "watcherGlob": "**"
  },
  "devDependencies": {
    "parcel-plugin-static-files-copy": "^2.6.0"
  }
}